/**
 * Capa de infraestructura del modulo Access
 *
 * Actualmente vacia. El modulo Access es un Gateway de lectura
 * que no tiene adaptadores propios - utiliza los ports de otros
 * dominios (ISessionQuery, IRestrictionQuery).
 *
 * Esta capa existe para cumplir con la segmentacion vertical
 * obligatoria segun daRulez 2.2, y para futuras extensiones
 * como cache de estado o metricas de acceso.
 */

// Reservado para futuros adaptadores de infraestructura

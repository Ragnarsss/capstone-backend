# Production stage - using tsx for runtime TypeScript execution
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including tsx for runtime)
RUN npm ci

# Copy source code
COPY . .

EXPOSE 3000

# Run TypeScript directly with tsx (no build step needed)
CMD ["npm", "start"]
